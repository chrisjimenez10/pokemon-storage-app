<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokemon: <%= pokemon.name %></title>
    <link rel="stylesheet" href="../css/show.css" />
</head>
<body>
    <nav>
        <a href="/">Home Page</a>
        <a href="/pokemon">Pokemon</a>
    </nav>

    <main>
        <h1><%= pokemon.name %></h1>

        <img src="<%= pokemon.image %>" width="200px"/>

        <ul>
            <li>Strength: <%= pokemon.strength %></li>
            <li>Type: <%= pokemon.type %></li>
            <li>Ability: <%= pokemon.ability %></li>
            <li>Special Move: <%= pokemon.move %></li>
            <li>Pokeball: <%= pokemon.pokeball %></li>
            <li>Evolution: <%= pokemon.evolution === true ? "Yes" : "No" %></li>
        </ul>
    </main>

    <div class="btn-container">
        <form action="/pokemon/<%= pokemon._id %>?_method=DELETE" method="POST">
            <button id="delete-btn">Delete</button>
        </form>
        <a href="/pokemon/<%= pokemon._id %>/edit" ><button id="edit-btn">Edit</button></a>
    </div>
    
</body>
</html>